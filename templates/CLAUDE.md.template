# swarm-test · {{PROJECT_NAME}}

This file is the **stable, human-maintained** context for the swarm. Keep it concise — agents reload it on every run. Detailed detection results live in `config.json`, learned rules in `memory/learned-rules.md`, and per-flow specs in `flows/*.md`.

@memory/learned-rules.md

## Project

- **Framework:** {{FRAMEWORK}}
- **Target URL:** {{TARGET_URL}}

## Critical flows

{{FLOWS_LIST}}

## Business rules

_Fill this in by hand. The analyst agent checks every screenshot against these rules._

- (e.g.) A user must verify their email before being allowed to checkout.
- (e.g.) Refunds above 200€ require a confirmation modal.
- (e.g.) The "Buy" button must be disabled while quantity = 0.

## Test accounts

_Provide credentials or fixtures the e2e agent should use._

- (e.g.) `swarmtest+demo@example.com` / password `swarmtest-demo`

## Known limitations

_Things the agents should NOT flag as anomalies._

- (e.g.) The maintenance banner is intentional during deploy windows.

## Detected routes (top 30)

{{ROUTES_LIST}}

## Recent commits

```
{{GIT_RECENT}}
```
